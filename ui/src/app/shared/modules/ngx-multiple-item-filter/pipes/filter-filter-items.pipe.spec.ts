import { FilterFilterItemsPipe } from './filter-filter-items.pipe';

describe('FilterFilterItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFilterItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
