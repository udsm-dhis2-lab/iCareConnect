import { FilterFormsByLocationPipe } from './filter-forms-by-location.pipe';

describe('FilterFormsByLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFormsByLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
