import { FilterLocationByServicePipe } from './filter-location-by-service.pipe';

describe('FilterLocationByServicePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLocationByServicePipe();
    expect(pipe).toBeTruthy();
  });
});
