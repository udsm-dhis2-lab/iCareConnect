import { FilterServicesConceptPipe } from './filter-services-concept.pipe';

describe('FilterServicesConceptPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterServicesConceptPipe();
    expect(pipe).toBeTruthy();
  });
});
