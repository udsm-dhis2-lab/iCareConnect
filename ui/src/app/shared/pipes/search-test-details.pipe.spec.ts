import { SearchTestDetailsPipe } from './search-test-details.pipe';

describe('SearchTestDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTestDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
