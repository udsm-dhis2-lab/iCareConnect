import { FilterItemsBySelectionsPipe } from './filter-items-by-selections.pipe';

describe('FilterItemsBySelectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemsBySelectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
