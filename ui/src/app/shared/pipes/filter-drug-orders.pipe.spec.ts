import { FilterDrugOrdersPipe } from './filter-drug-orders.pipe';

describe('FilterDrugOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDrugOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
