import { FilterDiagnosesPipe } from './filter-diagnoses.pipe';

describe('FilterDiagnosesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDiagnosesPipe();
    expect(pipe).toBeTruthy();
  });
});
