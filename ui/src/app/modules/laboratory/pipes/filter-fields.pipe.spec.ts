import { FilterFieldsPipe } from './filter-fields.pipe';

describe('FilterFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
