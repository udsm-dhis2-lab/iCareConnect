import { FilterAuthorizationStatusesPipe } from './filter-authorization-statuses.pipe';

describe('FilterAuthorizationStatusesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAuthorizationStatusesPipe();
    expect(pipe).toBeTruthy();
  });
});
