import { SearchingItemPipe } from './searching-item.pipe';

describe('SearchingItemPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchingItemPipe();
    expect(pipe).toBeTruthy();
  });
});
